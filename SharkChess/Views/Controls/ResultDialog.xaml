<Window x:Class="SharkChess.Views.Controls.ResultDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="对局结束" 
        Height="320" Width="380"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">
    
    <Window.Resources>
        <!-- 结果颜色资源 -->
        <SolidColorBrush x:Key="RedWinColor" Color="#DC143C"/>
        <SolidColorBrush x:Key="BlackWinColor" Color="#333333"/>
        <SolidColorBrush x:Key="DrawColor" Color="#4A90D9"/>
        
        <!-- 按钮样式 -->
        <Style x:Key="DialogButton" TargetType="Button">
            <Setter Property="Background" Value="#3c3c3c"/>
            <Setter Property="Foreground" Value="#ffffff"/>
            <Setter Property="BorderBrush" Value="#555555"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#4a4a4a"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#333333"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- 主按钮样式（保存棋谱） -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource DialogButton}">
            <Setter Property="Background" Value="#4A90D9"/>
            <Setter Property="BorderBrush" Value="#3A7BC8"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5AA0E9"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#3A7BC8"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <!-- 主容器带圆角和阴影 -->
    <Border CornerRadius="8" Background="#2d2d30" BorderBrush="#3e3e42" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="Black" Direction="270" ShadowDepth="5" Opacity="0.5" BlurRadius="15"/>
        </Border.Effect>
        
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- 关闭按钮 -->
            <Button Grid.Row="0" Content="X" 
                    HorizontalAlignment="Right" 
                    VerticalAlignment="Top"
                    Background="Transparent"
                    Foreground="#888888"
                    BorderThickness="0"
                    FontSize="14"
                    Padding="5"
                    Cursor="Hand"
                    Click="CloseButton_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <ContentPresenter HorizontalAlignment="Center" 
                                                    VerticalAlignment="Center"/>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#ff6b6b"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            
            <!-- 结果标题 -->
            <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,10,0,15">
                <TextBlock x:Name="ResultText" 
                          Text="红方胜" 
                          FontSize="28" 
                          FontWeight="Bold"
                          Foreground="{StaticResource RedWinColor}"
                          HorizontalAlignment="Center"/>
                <TextBlock x:Name="ReasonText" 
                          Text="将死" 
                          FontSize="14" 
                          Foreground="#888888"
                          HorizontalAlignment="Center"
                          Margin="0,5,0,0"/>
            </StackPanel>
            
            <!-- 对局信息 -->
            <Border Grid.Row="2" Background="#1e1e1e" CornerRadius="4" Padding="15,10" Margin="0,0,0,15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- 红方 -->
                    <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                        <TextBlock Text="红方" FontSize="12" Foreground="#ff6b6b" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="RedTimeText" 
                                  Text="00:00" 
                                  FontSize="18" 
                                  FontWeight="Bold"
                                  Foreground="#cccccc"
                                  HorizontalAlignment="Center"
                                  Margin="0,5,0,0"/>
                    </StackPanel>
                    
                    <!-- 分隔线 -->
                    <Border Grid.Column="1" Width="1" Background="#3e3e42" Margin="20,0"/>
                    
                    <!-- 黑方 -->
                    <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                        <TextBlock Text="黑方" FontSize="12" Foreground="#888888" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="BlackTimeText" 
                                  Text="00:00" 
                                  FontSize="18" 
                                  FontWeight="Bold"
                                  Foreground="#cccccc"
                                  HorizontalAlignment="Center"
                                  Margin="0,5,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
            
            <!-- 步数信息 -->
            <TextBlock Grid.Row="3" 
                      x:Name="MovesText"
                      Text="共 0 步" 
                      FontSize="12" 
                      Foreground="#888888"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
            
            <!-- 操作按钮 -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="保存棋谱" 
                        Style="{StaticResource PrimaryButton}"
                        Click="SaveGame_Click"
                        Margin="0,0,10,0"/>
                <Button Content="再来一局" 
                        Style="{StaticResource DialogButton}"
                        Click="NewGame_Click"
                        Margin="10,0,10,0"/>
                <Button Content="返回大厅" 
                        Style="{StaticResource DialogButton}"
                        Click="ReturnToLobby_Click"
                        Margin="10,0,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
